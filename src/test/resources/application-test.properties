# Test Profile Configuration - DO NOT include spring.profiles.active here

# Minimal logging during tests
logging.level.root=WARN
logging.level.com.epam.book_review_svc=INFO

# Disable Swagger UI in tests
springdoc.swagger-ui.enabled=false

# Use in-memory/test data storage
app.data.file.location=classpath:fixtures/test-books.json


